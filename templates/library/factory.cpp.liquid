#include "factory.h"

{% for interface in module.interfaces %}
#include "{{interface.name|lower}}.h"
{% endfor %}

Factory::Factory(QObject *parent)
    : QObject(parent)
{
}

{% for interface in module.interfaces %}

Abstract{{interface.name}}* Factory::create{{interface.name|upperFirst}}(QObject *parent)
{
    return new {{interface.name}}(parent);
}

{% endfor %}
