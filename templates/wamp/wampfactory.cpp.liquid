#include "wampfactory.h"

{% for interface in module.interfaces %}
#include "wamp{{interface.name|lower}}.h"
{% endfor %}

WampFactory::WampFactory(QObject *parent)
    : QObject(parent)
{
    qDebug() << Q_FUNC_INFO;
}

{% for interface in module.interfaces %}

Abstract{{interface.name}}* WampFactory::create{{interface.name|upperFirst}}(QObject *parent)
{
    qDebug() << Q_FUNC_INFO;
    return new Wamp{{interface.name}}(parent);
}

{% endfor %}
